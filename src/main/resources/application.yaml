spring:
  datasource:
    url: jdbc:mysql://localhost:3306/my_java_db
    username: tskumar328@gmail.com
    password: Ytrewq123456$1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

#  jpa.hibernate.ddl-auto: update # Can be 'create', 'update', 'validate', 'none'